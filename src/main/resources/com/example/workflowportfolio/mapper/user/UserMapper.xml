<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.workflowportfolio.user.mapper.UserMapper">

<select id = "findById"
		parameterType = "long"
		resultType = "com.example.workflowportfolio.user.entity.User">

SELECT
	id,
	login_id,
	password,
	name,
	role_id,
	position_id,
	created_at,
	updated_at
FROM
	users
WHERE
	id = #{id}

</select>

<select id = "findByLoginId"
		parameterType = "java.lang.String"
		resultType = "com.example.workflowportfolio.user.entity.User">

SELECT
	id,
	login_id,
	password,
	name,
	role_id,
	position_id,
	created_at,
	updated_at
FROM
	users
WHERE
	login_id = #{loginId}
	
</select>

<select id = "findAll"
		resultType = "com.example.workflowportfolio.user.entity.User">

SELECT
	id,
	login_id,
	password,
	name,
	role_id,
	position_id,
	created_at,
	updated_at
FROM
	users

</select>

<insert id="insert"
		parameterType="com.example.workflowportfolio.user.entity.User">

INSERT INTO users(
	login_id,
	password,
	name,
	role_id,
	position_id
	)
VALUES(
	#{loginId},
	#{password},
	#{name},
	#{roleId},
	#{positionId}
	)

</insert>

</mapper>